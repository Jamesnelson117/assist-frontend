import { RequestsFilterPipe } from './requests-filter-pipe.pipe';

describe('RequestsFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new RequestsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
